const Store = {
    data: {
        darkMode: false,
        back: false
    },
    methods: {
        timeOutHandler() {
            alert("Failed to connect");
        }
    }
};

export default Store;